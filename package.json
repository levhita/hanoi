{
  "name": "hanoi",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./app.js",
    "build:digitalocean": "cd ./frontend/ && npm install --production=false && npm run build && npm ci"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
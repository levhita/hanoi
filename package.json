{
  "name": "hanoi",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./app.js",
    "build:digitalocean": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}